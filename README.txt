VICTORIA FC - FIREBASE CONNECTED

Upload the contents of this folder to the GitHub repository root.
Firebase project: victori4-fc

Application submission uses a Firestore batch so the application, 7-day phone lock and status lookup are created atomically.
If submission fails, the site now displays the exact Firebase error code/message instead of hiding it behind a generic message.
